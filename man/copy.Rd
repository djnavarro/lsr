% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colCopy.R, R/rowCopy.R
\name{copy}
\alias{copy}
\alias{colCopy}
\alias{rowCopy}
\title{Copy a vector into a matrix}
\usage{
colCopy(x, times, dimnames = NULL)

rowCopy(x, times, dimnames = NULL)
}
\arguments{
\item{x}{A vector to be copied.}

\item{times}{The number of copies to stack together.}

\item{dimnames}{An optional list specifying row and column names for the
output matrix.}
}
\value{
For \code{rowCopy}, a matrix with \code{times} rows and
\code{length(x)} columns, where each row is \code{x}. For \code{colCopy},
a matrix with \code{length(x)} rows and \code{times} columns, where each
column is \code{x}.
}
\description{
Creates a matrix by stacking multiple copies of a vector as
rows (\code{rowCopy}) or as columns (\code{colCopy}).
}
\details{
These functions are shortcuts for building a matrix where every
row (or every column) is the same vector. They are equivalent to calling
\code{\link{matrix}} with appropriate \code{byrow} and dimension arguments.
}
\examples{
x <- c(3, 1, 4, 1, 5)

# stack x as rows
rowCopy(x, 4)

# stack x as columns
colCopy(x, 4)

# with custom dimension names
dnames <- list(rows = c("r1", "r2", "r3"), cols = c("c1", "c2", "c3", "c4", "c5"))
rowCopy(x, 3, dnames)
}
\seealso{
\code{\link{matrix}}, \code{\link{rbind}}, \code{\link{cbind}}
}
