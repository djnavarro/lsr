% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importList.R
\name{importList}
\alias{importList}
\title{Import a list into the workspace}
\usage{
importList(x, ask = TRUE)
}
\arguments{
\item{x}{A list or data frame whose elements are to be imported as
individual variables.}

\item{ask}{Set to \code{TRUE} (the default) to display the names of the
variables that will be created and ask for confirmation before
proceeding. Set to \code{FALSE} to import silently.}
}
\value{
Called primarily for its side effect of creating variables in the
workspace. Invisibly returns \code{1} if variables were created, \code{0}
if the user declined.
}
\description{
Copies each element of a list into a separate variable in the
workspace, using the list element names as variable names.
}
\details{
Creates one variable per list element in the calling environment
(usually the global workspace). All elements of \code{x} must be named;
passing an unnamed or partially-named list is an error. Element names that
are not valid R variable names are automatically converted using
\code{\link{make.names}}. An empty list produces a message and returns
invisibly without creating any variables.
}
\examples{
values <- c(1, 2, 3, 4, 5)
group <- c("group A", "group A", "group B", "group B", "group B")

# split() returns a named list: one element per group
grp_list <- split(values, group)

# import silently (no confirmation prompt)
importList(grp_list, ask = FALSE)

if (FALSE) {
  # interactive: shows variable names and asks for confirmation
  importList(grp_list)
}

}
\seealso{
\code{\link{unlist}}, \code{\link{attach}}
}
